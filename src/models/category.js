export class Categoria {
  constructor(id, title, icon, color, description, created_at, status, []){
    this.id = id,
    this.title = title,
    this.icon = icon,
    this.color = color,
    this.description = description,
    this.created_at = created_at,
    this.status = status,
    this.task = []
  }
} 
