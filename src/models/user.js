
export default class User {
  constructor(id, email, name, password, status){
    this.id = id,
    this.email = email,
    this.name = name,
    this.password = password,
    this.status = status
  }
} 
