export class Categoria {
  constructor(id, title, icon, description, created_at, status, task){
    this.id = id,
    this.title = title,
    this.icon = icon,
    this.description = description,
    this.created_at = created_at,
    this.status = status,
    this.task = task
  }
} 
